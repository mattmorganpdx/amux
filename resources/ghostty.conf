# amux default Ghostty configuration
#
# Force xterm-256color because Ghostty's resource directory resolver can
# false-positive on ncurses-shipped terminfo entries (e.g. on Arch Linux),
# causing TERM=xterm-ghostty which doesn't exist as a terminfo entry on
# most systems. xterm-256color is universally available and sufficient.
term = xterm-256color
